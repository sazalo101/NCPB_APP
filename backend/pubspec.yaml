name: ncpb_backend
description: NCPB Invoice Manager backend (SQLite + auth)
version: 2.0.0
publish_to: 'none'

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  shelf: ^1.4.1
  shelf_router: ^1.1.4
  shelf_multipart: ^1.0.0
  uuid: ^4.3.3
  mime: ^1.0.4
  sqlite3: ^2.4.6
  crypto: ^3.0.3

dev_dependencies:
  lints: ^3.0.0
